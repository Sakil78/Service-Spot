# ===========================================
# ServiceSpot Environment Variables
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control!
# The .env file is in .gitignore for your protection

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# MySQL database credentials (REQUIRED)
DATABASE_URL=jdbc:mysql://localhost:3306/service_spot?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DATABASE_USERNAME=root
DATABASE_PASSWORD=your_mysql_password_here

# ===========================================
# JWT CONFIGURATION
# ===========================================
# JWT secret key for token signing
# Generate a strong key: openssl rand -hex 32
JWT_SECRET=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# ===========================================
# EMAIL CONFIGURATION (Gmail SMTP - FREE!)
# ===========================================
# Gmail account for sending emails (password reset, notifications)
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-16-char-app-password-here
EMAIL_FROM=your-email@gmail.com

# ========================================
# How to get Gmail App Password:
# ========================================
# 1. Go to: https://myaccount.google.com/security
# 2. Enable 2-Factor Authentication (if not enabled)
# 3. Go to: https://myaccount.google.com/apppasswords
# 4. Select app: "Mail" and device: "Other (Custom name)"
# 5. Name it: "ServiceSpot"
# 6. Copy the 16-character password (remove spaces!)
# 7. Paste above in EMAIL_PASSWORD
#
# Email Features (ALL FREE!):
# - Password reset emails (6-digit codes)
# - Welcome emails after registration
# - Booking confirmations
# - Professional HTML templates
# - 500 emails/day limit
# - Works with ANY email provider for recipients

# ===========================================
# ADMIN CREDENTIALS (Development)
# ===========================================
# Default admin account (change for production!)
ADMIN_EMAIL=admin@servicespot.com
ADMIN_PASSWORD=Admin@123
ADMIN_NAME=System Administrator
ADMIN_PHONE=9999999999

# ===========================================
# SERVER CONFIGURATION
# ===========================================
SERVER_PORT=8080
# Backend URL: http://localhost:8080
# API Base URL: http://localhost:8080/api

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Allowed origins for Cross-Origin Resource Sharing
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://yourdomain.com

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
# Frontend development server (Vite)
VITE_API_BASE_URL=http://localhost:8080/api
FRONTEND_PORT=3000
# Frontend URL: http://localhost:3000

# ===========================================
# PRODUCTION DEPLOYMENT NOTES
# ===========================================
# 1. Generate strong JWT_SECRET: openssl rand -hex 32
# 2. Use strong ADMIN_PASSWORD (16+ chars, mixed case, numbers, symbols)
# 3. Update CORS_ALLOWED_ORIGINS with your production domain
# 4. Set DATABASE credentials for your production database server
# 5. Update EMAIL credentials with production Gmail account
# 6. Never commit .env file to Git!
# 7. On production servers (Railway, Render, etc.), set environment variables in dashboard
# 8. Use secure password managers to store credentials

# ===========================================
# 📋 QUICK REFERENCE
# ===========================================
# Frontend: http://localhost:3000
# Backend:  http://localhost:8080
# API:      http://localhost:8080/api
#
# Default Login:
# Email:    admin@servicespot.com
# Password: Admin@123

